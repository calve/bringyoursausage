<%= bootstrap_form_for(@barbecue_ingredient, layout: :inline) do |f| %>
  <% if @barbecue_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@barbecue_ingredient.errors.count, "error") %> prohibited this quantity from being saved:</h2>

      <ul>
      <% @barbecue_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    We need
    <%= f.number_field :quantity, hide_label: :true, placeholder: "2" %>
    <%= f.collection_select :ingredient_id, Ingredient.all, :id, :title, hide_label: :true %>
    at <%= @barbecue.title %>
    <p><%= link_to 'Your ingredient is not listed ? Create it first !', new_ingredient_path %></p>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
