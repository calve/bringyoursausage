<!DOCTYPE html>
<html>
  <head>
    <title>Bringyoursausage</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-bringyoursausage">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Bring your sausage !', root_url, class:"navbar-brand" %>
        </div>
        <div class="collapse navbar-collapse" id="navbar-bringyoursausage">
          <ul class="nav navbar-nav">
            <li <% if current_page?(:controller => '/barbecues', :method => 'new')%>class="active"<% end %>><%= link_to 'Create', new_barbecue_path %></li>
            <li <% if current_page?(:controller => '/barbecues')%>class="active"<% end %>><%= link_to 'Join', barbecues_path %></li>
            <% if user_signed_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name+' ('+current_user.email+')'%> <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
                  <li><%= link_to 'Logout', user_sign_out_path %></li>
                </ul>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Login
                  <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to 'Regular', new_user_session_path %></li>
                  <li><%= link_to 'Facebook', user_omniauth_authorize_path("facebook") %></li>
                </ul>
              </li>
              <li><%= link_to 'Sign up', new_user_registration_path, class: "btn btn-succes" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
    <br/><br/><br/>
    <small>Read the <a href="https://github.com/calve/bringyoursausage">source</a></small>
  </body>
</html>
