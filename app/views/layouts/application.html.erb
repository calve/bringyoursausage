<!DOCTYPE html>
<html>
  <head>
    <title>Bringyoursausage</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <%= link_to 'Bring your sausage !', root_url, class:"navbar-brand" %>
        </div>
        <div class="collapse navbar-collapse">
          <div class="nav navbar-nav">
            <ul class="nav navbar-nav">
              <li <% if current_page?(:controller => '/barbecues')%>class="active"<% end %>><%= link_to 'Barbecues', barbecues_path %></li>
              <li <% if current_page?(:controller => '/ingredients')%>class="active"<% end %>><%= link_to 'Ingredients', ingredients_path %></li>
              <% if user_signed_in? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name+' ('+current_user.email+')'%> <span class="caret"></span></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
                    <li><%= link_to 'Logout', user_sign_out_path %></li>
                  </ul>
              <% else %>
                  <li><%= link_to 'Login', new_user_session_path %></li>
                  <li><%= link_to 'Sign up', new_user_registration_path %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <div class="container">
      <%= yield %>
    </div>
    <br/><br/><br/>
    <small>Read the <a href="https://github.com/calve/bringyoursausage">source</a></small>
  </body>
</html>
